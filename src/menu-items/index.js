import dashboard from './dashboard';
import history from './history';
import register from './register';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
    items: [dashboard, history, register]
};

export default menuItems;
